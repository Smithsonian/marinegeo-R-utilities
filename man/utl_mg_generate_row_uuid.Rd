% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utl_mg_generate_row_uuid.R
\name{utl_mg_generate_row_uuid}
\alias{utl_mg_generate_row_uuid}
\title{Generate stable row UUIDs from identity columns}
\usage{
utl_mg_generate_row_uuid(data, table_id)
}
\arguments{
\item{data}{A data frame containing at least the identity columns defined
for \code{table_id} in \code{marinegeo_metadata$database_structure}.}

\item{table_id}{Character scalar. The MarineGEO table identifier (from
\code{marinegeo_metadata$data_index$table_id}). Used to look up which columns
carry \code{uuid_identity = TRUE} in \code{database_structure}.}
}
\value{
\code{data} with a \code{row_uuid} character column prepended as the first
column. Each value is a UUID-formatted string derived from a SHA1 hash of
the row's identity column values (e.g.,
\code{"3b4a1c2d-e5f6-7a8b-9c0d-e1f2a3b4c5d6"}).
}
\description{
Adds a \code{row_uuid} column to a data frame by hashing the values of the
columns designated as row identity for the given table. Identity columns are
defined in \code{marinegeo_metadata$database_structure} via the \code{uuid_identity}
logical flag.

The UUID is deterministic: the same combination of identity-column values
always produces the same \code{row_uuid}, making it safe to regenerate across
pipeline runs and to use as a stable join key.
}
\details{
\subsection{How UUIDs are generated}{

For each row, the values of the \code{uuid_identity} columns are coerced to
character and concatenated with \code{"|"} as a separator. That string is hashed
with SHA1 via \code{\link[digest:digest]{digest::digest()}} (\code{serialize = FALSE}). The first 32
hexadecimal characters of the hash are formatted as a standard UUID
(\code{8-4-4-4-12}).
}

\subsection{NA handling}{

\code{NA} values in identity columns are coerced to the string \code{"NA"} before
hashing. A row with all-\code{NA} identity columns will receive a valid UUID, but
it will be identical to every other such row — uniqueness is only guaranteed
when identity columns are non-\code{NA}.
}

\subsection{Overwriting an existing column}{

If \code{data} already contains a \code{row_uuid} column it will be dropped and
regenerated with a warning.
}
}
\examples{
db_struct <- data.frame(
  table_id      = c("sav_cover_v1", "sav_cover_v1", "sav_cover_v1"),
  column_name   = c("site_code", "transect_id", "percent_cover"),
  uuid_identity = c(TRUE, TRUE, FALSE),
  stringsAsFactors = FALSE
)

df <- data.frame(
  site_code     = c("BIS-001", "BIS-001"),
  transect_id   = c(1L, 2L),
  percent_cover = c(45.2, 30.1),
  stringsAsFactors = FALSE
)

# Not run (requires internal metadata):
# utl_mg_generate_row_uuid(df, table_id = "sav_cover_v1")
}
