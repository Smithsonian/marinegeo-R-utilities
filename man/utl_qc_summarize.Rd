% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utl_mg_qc_summarize.R
\name{utl_qc_summarize}
\alias{utl_qc_summarize}
\title{Summarize QC run results into tidy data frames}
\usage{
utl_qc_summarize(qc_result, type = "both")
}
\arguments{
\item{qc_result}{A named list as returned by \code{\link[=qc_run]{qc_run()}}. Must contain a
\verb{$tests} element (itself a named list of per-test result lists).}

\item{type}{Character scalar. Controls which summary objects are returned.
One of \code{"summary"}, \code{"failures"}, or \code{"both"} (default).}
}
\value{
A named list. Contents depend on \code{type}:
\itemize{
\item \code{"summary"} → \verb{list(summary = <data frame>)} with one row per test and
columns \code{test}, \code{status}, \code{message}, \code{n_failures}.
\item \code{"failures"} → \verb{list(failures = <data frame>)} with all per-test failure
rows bound together and a leading \code{test} column. Zero-row data frame when
there are no failures.
\item \code{"both"} → both of the above.
\item Empty \verb{$tests} → \code{list()} (with a message).
}
}
\description{
Flattens the nested list returned by \code{\link[=qc_run]{qc_run()}} into one or two tidy data
frames that are easier to read and display. Useful for reporting, logging,
and interactive review of QC results.
}
\details{
\strong{\code{n_failures} values in the summary table:}
\itemize{
\item \code{0L} when the test status is \code{"pass"}.
\item Row count of \verb{$failures} when the test produced a non-NULL, non-empty
failures data frame.
\item \code{NA_integer_} for non-passing tests where \verb{$failures} is \code{NULL}, which
occurs when \code{qc_run()} was called with \code{detail = FALSE}.
}

\strong{Failures table columns:}
Individual tests may record different columns in their \verb{$failures} data
frames. Before binding, all columns except \code{row_index} and \code{col_index} are
coerced to character to avoid type conflicts (e.g. when \code{value} is numeric
in one test and character in another). Columns not present in a given test
are filled with \code{NA}.
}
\examples{
result <- list(
  table_id = "demo",
  status = "fail",
  n_rows = 10L,
  tests = list(
    qc_check_columns = list(
      test = "qc_check_columns", status = "pass",
      message = "All columns present.",
      summary = data.frame(n_expected = 3L, n_present = 3L),
      failures = NULL
    ),
    qc_check_data_types = list(
      test = "qc_check_data_types", status = "fail",
      message = "2 type mismatches.",
      summary = data.frame(n_checked = 3L, n_type_mismatches = 2L),
      failures = data.frame(
        column_name = c("value", "date"),
        issue = c("type_mismatch", "type_mismatch")
      )
    )
  )
)

utl_qc_summarize(result)
utl_qc_summarize(result, type = "summary")
utl_qc_summarize(result, type = "failures")

}
