% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utl_mg_registry.R
\name{utl_mg_get_registry}
\alias{utl_mg_get_registry}
\title{Access MarineGEO global entity registry tables}
\usage{
utl_mg_get_registry(table, ...)
}
\arguments{
\item{table}{Character scalar. The registry table to retrieve. Must be one
of \code{"partner_codes"}, \code{"site_names"}, or \code{"observation_lookup"}.}

\item{...}{Named filter arguments. Each name must match a column in the
requested table, and each value is matched with \code{\%in\%}, so character
vectors can be supplied to match multiple values (e.g.,
\code{partner_code = c("USA-MDA", "BLZ-CBC")}). Multiple filter arguments are
applied with AND logic.}
}
\value{
A data frame (tibble) containing the requested table, filtered to
rows that satisfy all supplied filter conditions. Returns a 0-row data
frame with a \code{message()} if no rows match.
}
\description{
Returns a registry table from the internal \code{marinegeo_metadata} object,
optionally filtered to a subset of rows. Supported tables are
\code{"partner_codes"}, \code{"site_names"}, and \code{"observation_lookup"}.
}
\details{
\subsection{Column reference by table}{

\strong{\code{partner_codes}}
\itemize{
\item \code{partner_code} — Unique partner identifier (e.g., \code{"USA-MDA"}).
\item \code{name} — Human-readable partner name.
\item \code{institution} — Affiliated institution.
\item \code{country} — Country of the partner.
\item \code{type} — Partner type: \code{"observatory"} or \code{"project"}.
}

\strong{\code{site_names}}
\itemize{
\item \code{partner_code} — Links to \code{partner_codes}.
\item \code{site_name} — Human-readable site name.
\item \code{habitat} — Dominant habitat type (e.g., \code{"seagrass"}, \code{"coral reef"}).
\item \code{latitude} — Decimal latitude (may be \code{NA}).
\item \code{longitude} — Decimal longitude (may be \code{NA}).
}

\strong{\code{observation_lookup}}
\itemize{
\item \code{scientific_name} — Species or functional group name as entered in
MarineGEO datasets.
\item \code{scientific_id} — Unique identifier (Aphia ID or functional group ID).
Additional columns may be present depending on the version of the metadata.
}
}

\subsection{Filtering}{

Filter values are matched with \code{\%in\%}, so a character vector matches any of
the supplied values. All filter conditions must be satisfied simultaneously
(AND logic). An empty result produces a \code{message()} rather than a warning or
error.
}
}
\examples{
# Retrieve the full partner registry
# utl_mg_get_registry("partner_codes")

# Filter sites for a single partner
# utl_mg_get_registry("site_names", partner_code = "USA-MDA")

# Filter sites for multiple partners at once
# utl_mg_get_registry("site_names", partner_code = c("USA-MDA", "BLZ-CBC"))

# Combine filters (AND logic): seagrass sites for a specific partner
# utl_mg_get_registry("site_names", partner_code = "USA-MDA", habitat = "seagrass")

# Look up observation IDs for a known species
# utl_mg_get_registry("observation_lookup", scientific_name = "Zostera marina")
}
