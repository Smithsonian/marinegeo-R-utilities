% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utl_mg_taxonomy.R
\name{utl_mg_get_scientific_id}
\alias{utl_mg_get_scientific_id}
\title{Look up scientific IDs for a vector of scientific names}
\usage{
utl_mg_get_scientific_id(scientific_name, drop_abbreviations = TRUE)
}
\arguments{
\item{scientific_name}{Character vector of scientific names to look up.}

\item{drop_abbreviations}{Logical. If \code{TRUE} (default), trailing rank
abbreviations such as \code{sp.}, \code{spp.}, \verb{sp. A}, or \verb{sp. 1} are stripped
before the lookup so that e.g. \code{"Halodule sp."} resolves the same as
\code{"Halodule"}. The cleaned name is used for matching.}
}
\value{
A character vector the same length as \code{scientific_name}. Each
element is the matched \code{scientific_id}, or \code{NA} when a name could not be
found in \code{observation_lookup}. A warning lists any unmatched names.
}
\description{
Given a character vector of scientific names, returns the corresponding
\code{scientific_id} values from the MarineGEO \code{observation_lookup} table.
Designed for use inside \code{dplyr::mutate()} or standalone verification of a name vector.

\code{scientific_id} is the primary identifier linking observations across
MarineGEO metadata tables. It is either an Aphia ID (e.g. \code{"urn:lsid:marinespecies.org:taxname:123"}) for
species/taxonomic rank observations, a functional group ID (e.g.
\code{"FUNCTIONAL:SEAGRASS"}) for observations recorded at a functional group level,
or a temporary undefined label (`"UNDEFINED:SOMETHINGNOVEL"``)
}
\details{
The lookup is performed against \code{marinegeo_metadata$observation_lookup},
distributed with the package as internal data (see
\code{data-raw/assemble_marinegeo_metadata_sysdata.R}).

\code{NA} elements in \code{scientific_name} pass through as \code{NA} in the result
without triggering an unmatched-name warning.
}
\examples{
# Standalone verification
utl_mg_get_scientific_id(c("Halodule wrightii", "Thalassia testudinum"))

# Inside mutate()
dplyr::mutate(seagrass_cover_example,
              scientific_id = utl_mg_get_scientific_id(scientific_name))

# Abbreviations are stripped before matching
utl_mg_get_scientific_id("Halodule sp.")
}
