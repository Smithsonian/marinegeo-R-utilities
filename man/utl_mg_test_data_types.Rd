% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utl_mg_test_data_types.R
\name{utl_mg_test_data_types}
\alias{utl_mg_test_data_types}
\title{Validate column data types for Arrow-safe writing}
\usage{
utl_mg_test_data_types(df, table_id)
}
\arguments{
\item{df}{A data frame or tibble to validate.}

\item{table_id}{Character scalar. A MarineGEO versioned table identifier
(e.g., \code{"seagrass-cover-monitoring-v1"}). Must match a \code{table_id} present
in \code{marinegeo_metadata$database_structure}.}
}
\value{
Returns \code{df} invisibly if all checked columns pass type validation,
allowing the function to be used in a pipe chain without interrupting flow.
}
\description{
A pipe-friendly guardrail that validates column types in a data frame against
the MarineGEO schema for a given table, then throws a hard error if any
column violates what Apache Arrow expects. Place this function in a pipe
chain before a write operation (e.g., \code{\link[readr:write_csv]{readr::write_csv()}}) to block
incorrect data from being written.

Unlike \code{\link[=qc_check_data_types]{qc_check_data_types()}}, which returns a structured result list for
reporting, this function's purpose is to \strong{stop execution} when types are
wrong. It looks up the expected schema automatically from internal metadata
using \code{table_id}, so no manual type map is needed.
}
\details{
Only columns present in \strong{both} \code{df} and the metadata for \code{table_id} are
checked. Columns in \code{df} not in the metadata, and metadata columns absent
from \code{df}, are silently skipped.

SQL-style types from the metadata map to acceptable R types as follows:
\itemize{
\item \code{STRING}  — any R type is accepted (Arrow can coerce anything to
string)
\item \code{DOUBLE}  — numeric, integer, or logical
\item \code{INT} / \code{TINYINT} — integer or logical; numeric (double) only if all
non-NA values are whole numbers (i.e., \code{x == round(x, 0)})
\item \code{DATE}    — Date, POSIXct, or POSIXlt
\item \code{BOOL} / \code{BOOLEAN} — logical
}

All failures are collected before throwing a single error, so all type
violations are reported at once rather than stopping on the first.

\strong{All-NA logical columns}: A column stored as \code{logical} because all values
are \code{NA} (a common artifact of \code{read_csv()} or \code{read_excel()}) is treated as
a \code{message()} warning rather than a hard error, unless the expected type is
\code{BOOL} (in which case it passes normally).

\strong{Unknown SQL types}: Types not listed above (e.g., \code{TIMESTAMP}, \code{INT64})
are silently skipped.
}
\examples{
\dontrun{
my_data |>
  dplyr::filter(site_code == "BIS-001") |>
  dplyr::mutate(cover = as.numeric(cover)) |>
  utl_mg_test_data_types(table_id = "seagrass-cover-monitoring-v1") |>
  readr::write_csv("output.csv")
}
}
