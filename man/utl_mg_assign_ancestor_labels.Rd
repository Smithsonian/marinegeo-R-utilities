% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utl_mg_assign_ancestor_labels.R
\name{utl_mg_assign_ancestor_labels}
\alias{utl_mg_assign_ancestor_labels}
\title{Assign the nearest matching ancestor label to each scientific name}
\usage{
utl_mg_assign_ancestor_labels(fg_tree, scientific_names, ...)
}
\arguments{
\item{fg_tree}{Character scalar. Name of the functional group tree to query
(e.g. \code{"vegetation"}), matched against the \code{tree_name} column of
\code{functional_group_lookup}.}

\item{scientific_names}{Character vector of scientific names to classify,
typically a column from a data frame passed through \code{\link[dplyr:mutate]{dplyr::mutate()}}.
\code{NA} values are preserved as \code{NA} in the output without a message.}

\item{...}{One or more named \code{column = value} criteria identifying the
ancestor to return, e.g. \code{type = "primary"} or \code{rank = "Family"}. Each
name must be a column of \code{functional_group_lookup} other than \code{from},
\code{to}, or \code{tree_name}; each value must be a length-1, non-\code{NA} atomic
value. When several criteria are given they are combined with \strong{and} —
a single ancestor must satisfy all of them.}
}
\value{
A character vector of the same length as \code{scientific_names}. Each
element is the label of the nearest matching ancestor, or \code{NA_character_}
if:
\itemize{
\item The name is \code{NA}.
\item The name is not found in \code{observation_lookup} (a \code{warning()} is
raised by \code{\link[=utl_mg_get_scientific_id]{utl_mg_get_scientific_id()}}).
\item The name's identifier does not appear in \code{fg_tree}.
\item No node on the path from the name to the root satisfies \code{...}.
}
The last two cases are reported together in a single \code{message()} listing
the affected names.
}
\description{
For use inside \code{\link[dplyr:mutate]{dplyr::mutate()}}. Walks up a functional group tree from each
scientific name and returns the label of the \emph{nearest} ancestor whose tree
attributes match the criteria supplied in \code{...}.

Where \code{\link[=utl_mg_assign_functional_groups]{utl_mg_assign_functional_groups()}} requires you to enumerate the
candidate group labels up front, this function selects the ancestor by
matching against any column of the tree — so \code{type = "primary"} returns
whichever primary group the name falls under, and \code{rank = "Family"} returns
its family, without either label being known in advance.
}
\details{
Names are resolved to identifiers by \code{\link[=utl_mg_get_scientific_id]{utl_mg_get_scientific_id()}}, which
strips trailing rank abbreviations (e.g. \code{"sp."}, \code{"spp."}) before matching.
The tree is then built once and traversed for every name.

Matching is exact and case-sensitive, consistent with
\code{\link[=utl_mg_get_functional_groups]{utl_mg_get_functional_groups()}}. \code{NA} cells never match, so a node with no
\code{type} is never returned by \code{type = "primary"}.

The queried node itself is eligible: asking for \code{rank = "Species"} with a
species name returns that name's own label. Traversal then proceeds upward
and stops at the first — that is, the most specific — matching ancestor.

Root nodes are not matchable. The lookup table stores one row per \emph{child},
so a tree's root (e.g. \code{"Vegetation"}) has no row of its own and therefore
no attributes to match against.

Note that \code{rank} and \code{type} partition the tree: \code{rank} is populated only on
taxonomic nodes and is \code{NA} on \verb{FUNCTIONAL:} group nodes, while \code{type} and
\code{code} are populated only on curated group nodes.

Two WoRMS rank spellings are normalized before matching, mirroring
\code{\link[=utl_mg_get_taxonomic_classifications]{utl_mg_get_taxonomic_classifications()}}: \code{"Phylum (Division)"} is treated
as \code{"Phylum"} and \code{"Subphylum (Subdivision)"} as \code{"Subphylum"}, so
\code{rank = "Phylum"} also matches plant and algae lineages.
}
\examples{
# Nearest primary functional group
utl_mg_assign_ancestor_labels(
  fg_tree = "vegetation",
  scientific_names = c("Zostera marina", "Halodule wrightii"),
  type = "primary"
)

# Nearest ancestor at a given taxonomic rank
utl_mg_assign_ancestor_labels(
  fg_tree = "vegetation",
  scientific_names = "Zostera marina",
  rank = "Family"
)

# Inside mutate()
seagrass_cover_example |>
  dplyr::mutate(
    primary_group = utl_mg_assign_ancestor_labels(
      fg_tree = "vegetation",
      scientific_names = scientific_name,
      type = "primary"
    )
  )
}
\seealso{
\code{\link[=utl_mg_assign_functional_groups]{utl_mg_assign_functional_groups()}} to match against a known set of
candidate group labels instead.
}
