% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_mg_run.R
\name{qc_run}
\alias{qc_run}
\title{Run all applicable QC tests on a MarineGEO data table}
\usage{
qc_run(x, table_id, detail = TRUE, sheet = 1L)
}
\arguments{
\item{x}{A data frame, tibble, file path (character string), Arrow Dataset,
or Arrow Table.}

\item{table_id}{Character scalar. The MarineGEO table identifier (from
\code{marinegeo_metadata$data_index$table_id}). Used to look up expected
columns, data types, and controlled vocabularies.}

\item{detail}{Logical. If \code{TRUE} (default), failing rows are included in
each test result's \code{failures} element. If \code{FALSE}, \code{failures} is \code{NULL}
for all tests, reducing memory use in automated pipelines.}

\item{sheet}{Integer or character. Only used when \code{x} is an \code{.xlsx} or
\code{.xls} file path. Passed to \code{\link[readxl:read_excel]{readxl::read_excel()}}. Defaults to \code{1}.}
}
\value{
A named list with the following elements:
\describe{
\item{\code{table_id}}{The \code{table_id} argument.}
\item{\code{status}}{Character. The worst status across all tests:
\code{"fail"} > \code{"warn"} > \code{"pass"}.}
\item{\code{n_rows}}{Integer. Number of rows in the validated data.}
\item{\code{tests}}{Named list of individual test result lists, one per test
run. Each element has \code{test}, \code{status}, \code{message}, \code{summary}, and
\code{failures} (see \code{\link[=qc_check_columns]{qc_check_columns()}}, \code{\link[=qc_check_data_types]{qc_check_data_types()}},
\code{\link[=qc_check_categorical_values]{qc_check_categorical_values()}}, \code{\link[=qc_check_missing_values]{qc_check_missing_values()}},
\code{\link[=qc_check_numeric_ranges]{qc_check_numeric_ranges()}}, \code{\link[=qc_check_lookup_values]{qc_check_lookup_values()}}).}
}
}
\description{
Single entry point for the MarineGEO QA/QC test suite. Given a data object
and a \code{table_id}, \code{qc_run()} queries \code{marinegeo_metadata} to determine
which tests apply, assembles test parameters from metadata, and returns a
structured result list.

Accepts a data frame, a file path (CSV, XLSX, Parquet), or an Arrow
Dataset/Table.
}
\details{
\strong{Metadata-driven dispatch:} Tests are not hard-coded to table types.
Instead, the dispatcher queries \code{marinegeo_metadata} for the given
\code{table_id} and infers which tests to run:
\itemize{
\item Column presence/order test: runs if \code{database_structure} has rows
for \code{table_id}.
\item Data type test: runs if \code{database_structure} has rows with non-\code{NA}
\code{data_type} values for \code{table_id}.
\item Categorical values test: runs if \code{categorical_values} has rows for
\code{table_id}.
\item Missing values test: runs if \code{database_structure} has rows with
\code{missing_values} equal to \code{"enforce"} or \code{"warn"} for \code{table_id}.
\item Numeric ranges test: runs if \code{numeric_ranges} has rows with non-\code{NA}
\code{range_type} for \code{table_id}.
\item Lookup values test: runs if \code{data} contains any of the columns
\code{partner_code}, \code{site_name}, \code{site_code}, or \code{scientific_name}, which are
validated against the corresponding global entity registries in
\code{marinegeo_metadata} (\code{partner_codes}, \code{site_codes},
\code{observation_lookup}).
}
Adding a new table type requires only updating the metadata CSVs and
rebuilding sysdata — no R code changes required.

\strong{Arrow support:} Arrow Datasets and Tables are collected into memory via
\code{\link[dplyr:compute]{dplyr::collect()}} before testing. This requires the \code{arrow} package.

\strong{File path support:} CSV files are read with \code{\link[readr:read_delim]{readr::read_csv()}}; XLSX/XLS
files with \code{\link[readxl:read_excel]{readxl::read_excel()}}; Parquet files with
\code{\link[arrow:read_parquet]{arrow::read_parquet()}} (requires \code{arrow}).
}
\examples{
df <- data.frame(
  site_name       = "Example Site",
  survey_date     = as.Date("2024-06-01"),
  scientific_name = "Zostera marina",
  percent_cover   = 45.2
)

# Supply a valid table_id from marinegeo_metadata$data_index
# result <- qc_run(df, table_id = "sav_cover_v1")
}
