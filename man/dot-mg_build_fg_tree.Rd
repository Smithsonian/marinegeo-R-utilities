% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utl_mg_traverse_adjacency_tables.R
\name{.mg_build_fg_tree}
\alias{.mg_build_fg_tree}
\title{Build a \code{data.tree} object for one functional group tree}
\usage{
.mg_build_fg_tree(functional_group_tree)
}
\arguments{
\item{functional_group_tree}{Character scalar. Value of the \code{tree_name}
column identifying the tree to build (e.g. \code{"vegetation"}).}
}
\value{
A list with two elements:
\describe{
\item{fg}{The edge-list data frame filtered to \code{functional_group_tree},
in its original \code{from} = child / \code{to} = parent orientation, with a
\code{type} column guaranteed to be present.}
\item{tree}{The \code{data.tree} root node.}
}
}
\description{
Fetches \code{functional_group_lookup}, restricts it to a single tree, and builds
the corresponding \link[data.tree:Node]{data.tree::Node} object. Shared by
\code{\link[=utl_mg_get_functional_groups]{utl_mg_get_functional_groups()}} and \code{\link[=utl_mg_assign_ancestor_labels]{utl_mg_assign_ancestor_labels()}}.
}
\details{
The lookup table stores edges as \code{from} = child, \code{to} = parent, which is the
opposite of the \code{from} = parent, \code{to} = child convention
\code{\link[data.tree:FromDataFrameNetwork]{data.tree::FromDataFrameNetwork()}} expects. The two columns are therefore
swapped with \code{\link[dplyr:select]{dplyr::select()}} rather than \code{\link[dplyr:rename]{dplyr::rename()}}, because
\code{FromDataFrameNetwork()} reads the first two columns \emph{by position}.

A \code{type} column is added as all-\code{NA} when absent, so downstream callers can
reference it unconditionally.
}
\keyword{internal}
