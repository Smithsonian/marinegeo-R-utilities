% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_mg_timeseries.R
\name{viz_mg_timeseries_annual}
\alias{viz_mg_timeseries_annual}
\title{Plot annual time-series data with grouped lines}
\usage{
viz_mg_timeseries_annual(
  df,
  y_var,
  x_var = "year",
  y_label,
  y_limits = NULL,
  y_grouping_var,
  facet_var = NULL,
  facet_num_cols = NULL,
  plot_theme = NULL
)
}
\arguments{
\item{df}{A data frame containing the columns named by \code{x_var}, \code{y_var},
\code{y_grouping_var}, and (if supplied) \code{facet_var}.}

\item{y_var}{Character scalar. Name of the column in \code{df} to map to the
y-axis (the response variable).}

\item{x_var}{Character scalar. Name of the column in \code{df} to map to the
x-axis. Defaults to \code{"year"}. The column is treated as a discrete axis
via \code{\link[ggplot2:scale_x_discrete]{ggplot2::scale_x_discrete()}}.}

\item{y_label}{Character scalar. Label to display on the y-axis.}

\item{y_limits}{Numeric vector of length 2, \code{c(min, max)}, passed to
\code{\link[ggplot2:ylim]{ggplot2::ylim()}}. If \code{NULL} (default), axis limits are determined
automatically. \strong{Note:} data points outside the specified range are
silently dropped by ggplot2.}

\item{y_grouping_var}{Character scalar. Name of the column in \code{df} whose
unique values define the line groups (colour and line grouping).}

\item{facet_var}{Character scalar or \code{NULL} (default). Name of the column
in \code{df} to use for \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} panels. When \code{NULL}, no
faceting is applied.}

\item{facet_num_cols}{Positive integer or \code{NULL} (default). Number of
columns passed to \code{\link[ggplot2:facet_wrap]{ggplot2::facet_wrap()}} as \code{ncol}. Ignored when
\code{facet_var} is \code{NULL}.}

\item{plot_theme}{A complete ggplot2 theme object (e.g.,
\code{ggplot2::theme_bw()}) or \code{NULL} (default). When non-\code{NULL}, the theme
is added to the plot with \code{+}.}
}
\value{
A \code{ggplot} object. The caller is responsible for printing or saving
the plot (e.g., with \code{\link[ggplot2:ggsave]{ggplot2::ggsave()}}) or converting it to an
interactive widget with \code{\link[=viz_mg_ggplotly]{viz_mg_ggplotly()}}.
}
\description{
Creates a \code{ggplot2} line-and-point chart for visualising yearly MarineGEO
monitoring data. Each level of \code{y_grouping_var} is drawn as a separate
coloured line. Optionally, the chart can be faceted by a categorical
variable and y-axis limits can be fixed.

To render the result as an interactive plotly widget, pass the returned
object to \code{\link[=viz_mg_ggplotly]{viz_mg_ggplotly()}}.
}
\details{
The x-axis is rendered as a discrete scale so that years with no
observations are not interpolated. If \code{x_var} holds a numeric year column,
convert it to a factor or character before passing to this function.

\code{y_limits} is applied via \code{\link[ggplot2:ylim]{ggplot2::ylim()}}, which drops data points
outside the specified range. Use \code{\link[ggplot2:coord_cartesian]{ggplot2::coord_cartesian()}} directly on
the returned object if you need to zoom without dropping data.
}
\examples{
df <- data.frame(
  year    = factor(rep(2020:2022, each = 2)),
  cover   = c(10, 20, 15, 25, 12, 18),
  species = rep(c("Halodule wrightii", "Thalassia testudinum"), 3),
  site    = rep(c("Site A", "Site B", "Site A"), each = 2)
)

# Minimal call
viz_mg_timeseries_annual(
  df             = df,
  y_var          = "cover",
  y_label        = "Percent cover",
  y_grouping_var = "species"
)

# Faceted by site with fixed y limits and a theme
viz_mg_timeseries_annual(
  df             = df,
  y_var          = "cover",
  y_label        = "Percent cover",
  y_grouping_var = "species",
  facet_var      = "site",
  facet_num_cols = 2L,
  y_limits       = c(0, 100),
  plot_theme     = ggplot2::theme_bw()
)
}
