% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/db_marinegeo_L2.R
\name{db_arrow_marinegeo}
\alias{db_arrow_marinegeo}
\title{Connect to a MarineGEO Arrow dataset}
\usage{
db_arrow_marinegeo(table_id, return_type = "arrow_dataset")
}
\arguments{
\item{table_id}{Character. The versioned table identifier (e.g.,
\code{"reef-life-survey-data-marinegeo-v1"}). Must match a \code{table_id} in the
internal \code{marinegeo_metadata$data_index}.}

\item{return_type}{Character. Either \code{"arrow_dataset"} (default) to return
a lazy Arrow dataset, or \code{"dataframe"} to collect the full dataset into a
data frame immediately.}
}
\value{
An Arrow Dataset object when \code{return_type = "arrow_dataset"}, or a
data frame when \code{return_type = "dataframe"}. Returns \code{NULL} with a message
if \code{table_id} is not found in the data structure metadata.
}
\description{
Opens a controlled MarineGEO L2 data directory using Arrow. The target
\code{table_id} must be defined in the internal MarineGEO data index. Use
\code{return_type = "dataframe"} to load the full dataset into R immediately.
The default \code{return_type = "arrow_dataset"} returns a lazy Arrow dataset,
allowing you to filter before collecting into memory.
}
\details{
Column types are derived from the internal \code{marinegeo_metadata$database_structure}
table using SQL-style type names (\code{STRING}, \code{INT}, \code{INT64}, \code{DOUBLE}, \code{DATE},
\code{BOOL}, \code{TINYINT}, \code{TIMESTAMP}). Two columns receive special handling
regardless of the metadata: \code{timestamp_utc} is always cast to
\code{arrow::timestamp(timezone = "UTC")} and \code{timestamp_local} is always cast to
\code{arrow::string()}.

Dataset path is resolved from \code{marinegeo_metadata$data_index}. Currently
only \code{location = "github"} is supported; the path is constructed from the
\code{repository_filepath} environment variable and the \code{directory} field.
}
\examples{
\dontrun{
table_id <- "reef-life-survey-data-marinegeo-v1"
ds <- db_arrow_marinegeo(table_id)
ds |> dplyr::filter(site_code == "BC4") |> dplyr::collect()

# Load full dataset into memory
df <- db_arrow_marinegeo(table_id, return_type = "dataframe")
}
}
