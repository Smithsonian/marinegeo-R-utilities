% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utl_mg_get_taxonomic_classifications.R
\name{utl_mg_get_taxonomic_classifications}
\alias{utl_mg_get_taxonomic_classifications}
\title{Get wide-form taxonomic classifications for a set of scientific IDs}
\usage{
utl_mg_get_taxonomic_classifications(scientific_ids)
}
\arguments{
\item{scientific_ids}{Character vector of \code{scientific_id} values to look up
(e.g. \code{c("APHIA:374534", "APHIA:145792")}). \code{NA} values are removed with
a message.}
}
\value{
A data frame with one row per matched \code{scientific_id} and columns:
\describe{
\item{scientific_id}{The input identifier.}
\item{rank}{The taxonomic rank of the input ID itself (e.g. \code{"Species"}).}
\item{Kingdom, Phylum, Class, Order, Family, Genus, Species}{Taxon name
at each standard rank. Columns are only present when at least one row
has a value; missing ranks for a given row are \code{NA}.}
}
Returns a zero-row data frame with the expected columns if no IDs are
matched.
}
\description{
For each Aphia-based \code{scientific_id} supplied, walks the MarineGEO
\code{taxonomic_lookup} adjacency table to reconstruct the full taxonomic
hierarchy and returns the result as a wide data frame with one column per
standard rank.

Only IDs that begin with \code{"APHIA:"} are present in \code{taxonomic_lookup}.
Functional group IDs (\code{"FUNCTIONAL:"}) will not be matched and are silently
excluded from the output.
}
\details{
The lookup is performed against \code{marinegeo_metadata$taxonomic_lookup}, an
adjacency table distributed as internal package data. \code{Phylum (Division)} —
the WoRMS rank name used for some plant and algae lineages — is mapped to
\code{Phylum} before the standard columns are selected.

Intermediate ranks (Tribe, Forma, super-, sub-, infra-, mega-, giga-, parv-,
subter- prefixed ranks) are dropped from the output.
}
\examples{
df <- utl_mg_join_scientific_id(seagrass_cover_example)
utl_mg_get_taxonomic_classifications(unique(df$scientific_id))
}
