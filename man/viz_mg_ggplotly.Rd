% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/viz_mg_ggplotly.R
\name{viz_mg_ggplotly}
\alias{viz_mg_ggplotly}
\title{Convert a ggplot to an interactive plotly widget}
\usage{
viz_mg_ggplotly(plot, plotly_visible_traces = NULL)
}
\arguments{
\item{plot}{A \code{ggplot} object to convert.}

\item{plotly_visible_traces}{Character vector or \code{NULL} (default). Names of
traces that should be fully visible on initial render. All other traces are
set to \code{"legendonly"} (hidden in the plot but togglable via the legend).
Trace names correspond to the levels of the colour/group aesthetic in the
original ggplot. When \code{NULL}, all traces are visible.}
}
\value{
A \code{plotly} htmlwidget object.
}
\description{
Wraps \code{\link[plotly:ggplotly]{plotly::ggplotly()}} and optionally controls which traces are visible
by default in the legend. Designed to work with any \code{ggplot} object produced
by MarineGEO visualisation functions such as \code{\link[=viz_mg_timeseries_annual]{viz_mg_timeseries_annual()}}.
}
\details{
Trace names in a plotly object converted from ggplot2 match the values of
the grouping variable (e.g., species names, site codes). Use the exact
strings as they appear in the data column mapped to \code{color} or \code{group}.

Setting \code{"legendonly"} visibility hides a trace from the plot while keeping
it in the legend so users can click to show it. This is useful when a chart
has many groups and only a subset is relevant by default.
}
\examples{
df <- data.frame(
  year    = factor(rep(2020:2022, each = 2)),
  cover   = c(10, 20, 15, 25, 12, 18),
  species = rep(c("Halodule wrightii", "Thalassia testudinum"), 3)
)

p <- viz_mg_timeseries_annual(
  df             = df,
  y_var          = "cover",
  y_label        = "Percent cover",
  y_grouping_var = "species"
)

\dontrun{
# All traces visible
viz_mg_ggplotly(p)

# Only one species visible by default
viz_mg_ggplotly(p, plotly_visible_traces = "Halodule wrightii")
}
}
