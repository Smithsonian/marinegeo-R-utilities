% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utl_mg_backfill.R
\name{utl_sav_backfill_cover}
\alias{utl_sav_backfill_cover}
\title{Backfill seagrass cover data with zero-cover absence rows}
\usage{
utl_sav_backfill_cover(df)
}
\arguments{
\item{df}{A data frame containing seagrass cover observations. Must include
the following columns:
\describe{
\item{\code{scientific_name}}{Character. Species or taxon name; used to
determine functional group membership.}
\item{\code{sample_event_id}}{Character. Unique identifier for each sampling
event; used to group observations before expanding.}
\item{\code{partner_code}}{Character. MarineGEO partner identifier.}
\item{\code{site_name}}{Character. Site name.}
\item{\code{sample_collection_date}}{Date. Date of sample collection.}
\item{\code{transect}}{Transect identifier within a sample event.}
\item{\code{quadrat}}{Quadrat identifier within a transect.}
\item{\code{cover_method}}{Character. Method used to estimate cover.}
\item{\code{cover_quadrat_dimensions}}{Character. Dimensions of the cover
quadrat.}
\item{\code{input_filename}}{Character. Source file name.}
\item{\code{percent_cover}}{Numeric. Percent cover value; set to \code{0} for
backfilled rows.}
\item{\code{cover_code}}{Cover code value; set to \code{0} for backfilled rows.}
}}
}
\value{
A data frame with the same columns as \code{df}, sorted by
\code{sample_event_id}, year, \code{site_name}, \code{transect}, \code{quadrat}, and
\code{scientific_name}. Backfilled rows have \code{percent_cover = 0} and
\code{cover_code = 0}; all other columns for backfilled rows are filled with
the single value observed for that sample event, or \code{NA} with a
\code{message()} if multiple values are present.
}
\description{
Accepts a seagrass cover data frame and adds new rows to ensure that every
Seagrass or Macroalgae species observed anywhere within a sample event is
represented at every transect × quadrat combination within that event. The
\code{percent_cover} and \code{cover_code} for backfilled rows are set to \code{0}.
Non-macrophyte rows (functional group is neither Seagrass nor Macroalgae)
are passed through unchanged.
}
\details{
Functional group assignment is performed via
\code{\link[=utl_mg_assign_functional_groups]{utl_mg_assign_functional_groups()}} with \code{fg = c("Seagrass", "Macroalgae")}.
Rows whose \code{scientific_name} resolves to neither group (including unknowns
and non-macrophyte taxa) are collected in a separate data frame and
re-appended to the output without modification.

Within each sample event the function:
\enumerate{
\item Uses \code{\link[tidyr:expand]{tidyr::expand()}} with \code{\link[tidyr:expand]{tidyr::nesting()}} to produce all
combinations of \code{transect} × \code{quadrat} × \code{scientific_name} within the
event's existing transect–quadrat pairs.
\item Uses \code{\link[dplyr:filter-joins]{dplyr::anti_join()}} to identify combinations absent from the
original data and inserts them with \code{percent_cover = 0} and
\code{cover_code = 0}.
}

If \code{cover_method}, \code{cover_quadrat_dimensions}, or \code{input_filename} is not
unique within a sample event, the backfilled rows for that event receive
\code{NA} for the ambiguous field and a \code{message()} is emitted.
}
\examples{
\dontrun{
# seagrass_cover_example is a built-in package dataset
backfilled <- utl_sav_backfill_cover(seagrass_cover_example)
nrow(backfilled) >= nrow(seagrass_cover_example)  # TRUE
}
}
