% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_mg_issues.R
\name{qc_issues}
\alias{qc_issues}
\title{MarineGEO QC issues table}
\description{
The object returned by \code{\link[=qc_run]{qc_run()}} and every \verb{qc_check_*()} function. It is a
\link[tibble:tibble]{tibble} (class \code{qc_issues}) with one row per detected
problem (zero rows means the data passed the check) and the following
columns:
\describe{
\item{\code{check}}{Character. Name of the producing check, e.g.
\code{"qc_check_numeric_ranges"}.}
\item{\code{severity}}{Character. \code{"fail"} or \code{"warn"}.}
\item{\code{row}}{Integer. 1-based row number in the validated data, or \code{NA}
for table- or column-level issues (e.g. a missing column).}
\item{\code{column}}{Character. Offending column name, or \code{NA}.}
\item{\code{col_index}}{Integer. 1-based column position, or \code{NA}.}
\item{\code{value}}{Character. The offending value, or \code{NA}.}
\item{\code{issue}}{Character. Machine-readable code, e.g. \code{"out_of_range"}.}
}

Because it is an ordinary tibble, results are inspected and subset with
standard \code{dplyr} (e.g. \code{dplyr::filter()}, \code{dplyr::count()}) and printed like
any other tibble.

Run-level metadata is attached as attributes, read with \code{\link[base:attr]{base::attr()}}:
\code{table_id}, \code{n_rows} (rows in the validated data), \code{checks_run} (checks that
executed), and \code{status} (the worst severity present: \code{"fail"} > \code{"warn"} >
\code{"pass"}).
}
\keyword{internal}
