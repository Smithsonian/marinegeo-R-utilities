% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utl_mg_get_functional_groups.R
\name{.find_functional_groups}
\alias{.find_functional_groups}
\title{Walk the functional group tree to find all ancestor nodes for an ID}
\usage{
.find_functional_groups(scientific_id, tree)
}
\arguments{
\item{scientific_id}{Character scalar. The identifier to search for.}

\item{tree}{Named list. The \code{functional_group_enrollment} nested tree as
produced by \code{.build_functional_group_enrollment()}.}
}
\value{
An unnamed list of named lists, each with elements \code{id} (character),
\code{name} (character), and \code{depth} (integer, 1 = root). Results are ordered
root-first (ascending depth). Returns an empty list if \code{scientific_id} is
not found anywhere in the tree.
}
\description{
Recursively walks the \code{functional_group_enrollment} nested tree, collecting
every node that is an ancestor of \code{scientific_id}. The queried ID may be an
APHIA: species (found in a node's \code{members}), or any node ID present in the
tree itself (FUNCTIONAL: or APHIA: anchor node).

The queried node is never included in its own results — only ancestors are
returned.
}
\keyword{internal}
